<parent:PrintAreaViewerClass xmlns="js.svg"
                             xmlns:parent="hip.svg" xmlns:js="js.core"
                             translateX="{printArea.offset.x}"
                             translateY="{printArea.offset.y}">

    <!--<pattern id="muster_a" patternUnits="userSpaceOnUse" width="308" x="0"-->
             <!--y="0">-->
        <!--<image href="http://img.pr0gramm.com/2015/09/29/fa8ecc8cd5549029.jpg" width="100%"/>-->
    <!--</pattern>-->

    <rect width="{printArea.size.width}"
          cid="border"
          height="{printArea.size.height}"
          fill="none"
            />

    <g cid="configurationContainer"/>

    <g _v="{activeViewer}" visible="{showActiveViewer}" translateX="{_v._realOffset.x}" translateY="{_v._realOffset.y}"
       _handleDistance="{multiply(handleWidth,0.9)}"
       _fontSize="{multiply(handleWidth, 1.3)}"
       class="needsClick">


        <g translateX="{plus(_v._size.width,_handleDistance)}" translateY="{plus(_v._size.height,_handleDistance)}"
           rotation="90"
           onpointerdown="handlePointerDown('resize','rb',event,true)"
           class="configuration-handle">
            <circle r="{multiply(handleWidth, 1.4)}"/>
            <text y="{multiply(handleWidth,0.5)}" font-size="{_fontSize}" text-anchor="middle">
                &#xf065;
            </text>
        </g>

        <g translateX="{plus(_v._size.width,_handleDistance)}" translateY="{minus(0,_handleDistance)}"
           onpointerdown="handlePointerDown('move','b',event,true)"
                class="configuration-handle">
            <circle r="{multiply(handleWidth, 1.4)}"/>
            <text y="{multiply(handleWidth,0.5)}"
                  font-size="{_fontSize}"
                  text-anchor="middle">
                &#xf047;
            </text>
        </g>

        <g translateX="{minus(0,_handleDistance)}" translateY="{minus(0,handleWidth)}"
           onclick="removeConfiguration"
           class="configuration-handle configuration-handle__delete">
            <circle r="{multiply(handleWidth, 1.4)}"/>
            <text y="{multiply(handleWidth,0.5)}"
                  text-anchor="middle"
                  font-size="{_fontSize}">
                &#xf014;
            </text>
        </g>

        <g translateX="{minus(0,_handleDistance)}" translateY="{plus(_v._size.height,_handleDistance)}"
           onclick="cloneConfiguration"
           class="configuration-handle">
            <circle r="{multiply(handleWidth, 1.4)}"/>
            <text y="{multiply(handleWidth,0.5)}"
                  text-anchor="middle"
                  font-size="{_fontSize}">
                &#xf0c5;
            </text>
        </g>

        <g translateX="{half(_v._size.width)}" translateY="{plus(_v._size.height,multiply(handleWidth,1.4))}"
           onclick="editConfiguration"
           class="configuration-handle">
            <circle r="{multiply(handleWidth, 1.5)}"/>
            <text y="{multiply(handleWidth,0.5)}"
                  text-anchor="middle"
                  font-size="{multiply(handleWidth, 1.6)}">
                &#xf1fc;
            </text>
        </g>

    </g>

    <g cid="snapLines">
        <line x1="0" x2="0" y1="0" y2="0" stroke-opacity="0" stroke="aqua"/>
        <line x1="0" x2="0" y1="0" y2="0" stroke-opacity="0" stroke="aqua"/>
    </g>

</parent:PrintAreaViewerClass>